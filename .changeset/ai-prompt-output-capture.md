---
'evlog': minor
---

Add a `content` option to the AI SDK integration. When enabled, the wide event carries `ai.prompt` (the formatted prompt sent to the first model call) and `ai.output` (the text generated by the last model call), with optional `maxLength` truncation and a `transform` hook for redaction. Off by default.
