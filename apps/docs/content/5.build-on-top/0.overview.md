---
title: Build on top of evlog
description: How to extend evlog along five orthogonal axes — sources, pipeline, sinks, observers, and shared packages — from a solo dev's local debug tool to a platform team's shared catalogs.
navigation:
  title: Overview
  icon: i-lucide-layers
---

Everything you build on top of evlog falls on one of **four axes** in the data flow, plus a fifth transverse axis for code you want to **share between projects**.

```text
                                                          ┌─────────────────────┐
                                                          │  SOURCES            │  How events enter
                                                          │  (frameworks,       │  the pipeline
                                                          │   custom middleware)│
                                                          └──────────┬──────────┘
                                                                     │
                                                                     ▼
                                                          ┌─────────────────────┐
                              OBSERVERS                   │  PIPELINE           │  What happens to them
                              (read-only:        ◀────────│  (plugins,          │  on the way through
                              streams, fs reader)         │   enrichers,        │
                                                          │   sampling)         │
                                                          └──────────┬──────────┘
                                                                     │
                                                                     ▼
                                                          ┌─────────────────────┐
                                                          │  SINKS              │  Where they go
                                                          │  (drains: Axiom,    │
                                                          │   Datadog, fs, …,   │
                                                          │   custom)           │
                                                          └─────────────────────┘

                              SHARED PACKAGES (catalogs, drains, enrichers, integrations
                              packaged as reusable npm libraries — orthogonal to all four axes)
```

::callout
---
icon: i-lucide-info
---
**Use this page as a directory.** Pick the question that matches your need and follow the link. Each axis page goes deeper on its surface; each scenario page bundles a few axes for a specific real-world job.
::

## I want to…

| Question | Answer |
| --- | --- |
| Add a service that receives my logs (Axiom, an internal API…) | [Custom drains](/build-on-top/sinks/custom-drains) |
| Tint events with my own context (tenant id, deploy id, region…) | [Custom enrichers](/build-on-top/pipeline/custom-enrichers) |
| Decide live whether to keep an event based on the request outcome | [Tail sampling](/build-on-top/pipeline/tail-sampling) |
| React to specific events (alert, side-effect, tracing) | [Plugins](/build-on-top/pipeline/plugins) |
| Combine several drains with retry, batching, fanout | [Drain pipeline](/build-on-top/sinks/drain-pipeline) |
| See events live while I dev | [Stream server](/build-on-top/observers/stream-server) |
| Subscribe in-process from a script or test | [Stream API](/build-on-top/observers/in-process-stream) |
| Replay or tail historic logs | [FS reader](/build-on-top/observers/fs-reader) |
| Identify my evlog traffic on the receiver side | [Identity headers](/build-on-top/sinks/identity-headers) |
| Support a framework that's not in the list | [Custom framework](/build-on-top/sources/custom-framework) |
| Share an error vocabulary between several services | [Catalogs as packages](/build-on-top/shared/catalogs-as-packages) |
| Build a quick local devtool | [Local debugging toolkit](/build-on-top/scenarios/local-debugging-toolkit) |
| Handle multi-tenant context everywhere | [Tenant-aware logging](/build-on-top/scenarios/tenant-aware-logging) |
| Wire compliance-grade auditing | [Compliance audit](/build-on-top/scenarios/compliance-audit) |

## By stage of your app

| Stage | Typical needs | Pages |
| --- | --- | --- |
| **Solo / hobby** — one app, local dev | Live events while debugging, log replay | [Stream server](/build-on-top/observers/stream-server), [FS reader](/build-on-top/observers/fs-reader), [Local debugging toolkit](/build-on-top/scenarios/local-debugging-toolkit) |
| **Small team** — one app in production | Send to a provider, redact PII, tag traffic | [Sinks overview](/build-on-top/sinks), [Identity headers](/build-on-top/sinks/identity-headers) |
| **Scale** — single product, real prod | Custom enrichers, tail sampling, plugins, multi-drain fanout | [Pipeline overview](/build-on-top/pipeline), [Sinks overview](/build-on-top/sinks), [Tenant-aware logging](/build-on-top/scenarios/tenant-aware-logging) |
| **Platform** — N apps sharing infra | Shared catalogs, custom drains/enrichers/integrations as npm packages | [Shared packages](/build-on-top/shared) |

## By axis

### Sources — how events enter

[Sources overview](/build-on-top/sources)

`evlog/<framework>` middlewares plug HTTP requests into the pipeline. When you write your own framework support (a new HTTP runtime, a CLI driver, a queue worker), you live here.

### Pipeline — what happens during

[Pipeline overview](/build-on-top/pipeline)

Plugins, enrichers, sampling rules, redaction custom — anything that **transforms or decides** about an event between emit and drain.

### Sinks — where events go

[Sinks overview](/build-on-top/sinks)

Built-in drain adapters (Axiom, Datadog, fs, …) plus the building blocks to write your own: `defineDrain()`, `defineHttpDrain()`, the drain pipeline (batch + retry + fanout), and identity headers.

### Observers — read the flow

[Observers overview](/build-on-top/observers)

Subscribe to wide events without altering the pipeline. In-process pub/sub primitive, network SSE bridge, fs replay/tail, and concrete consumer recipes.

### Shared — package for others

[Shared packages overview](/build-on-top/shared)

Turn a catalog, a drain, an enricher, or a framework integration into an npm package others can install. Same pattern for each : a small bit of typed code + module augmentation.

## A note on serverless

The in-process [stream](/build-on-top/observers/in-process-stream) and the [stream server](/build-on-top/observers/stream-server) work everywhere a Node-like long-lived process runs — local dev, self-hosted servers, containers (Fly, Railway, Coolify), VMs.

They do **not** work on serverless platforms (Vercel Functions, Cloudflare Workers, AWS Lambda) because each invocation is an isolated process. Use a real broker (Redis Streams, NATS, Pub/Sub) for cross-instance fan-out in those environments.

The fs reader, identity headers, and the entire **Pipeline** / **Sinks** / **Shared** axes work everywhere — they are not bound to a long-lived process.
